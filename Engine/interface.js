
function __main__() {
    print("script loaded!");
}

function render()

// Standard Interface //
function print(content)
function IGraphic_drawLine(x1, y1, x2, y2, ax, ay, r)

function IGraphic_drawRect(left, top, right, bottom, rdsX, rdsY, filled, ax, ay, r)
